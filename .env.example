# LinkedIn needs no credentials here -- login is manual (a browser window opens
# once for you to log in yourself; only the resulting session is saved locally).

# Free-tier API keys (get from each provider's developer portal)
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
JOOBLE_API_KEY=

# Optional daily summary notifications
NOTIFY_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
NOTIFY_EMAIL_TO=
